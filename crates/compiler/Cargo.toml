[package]
name = "nexa-compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "1.0"
serde     = { version = "1",   features = ["derive"] }
bincode   = { version = "2.0", features = ["serde"] }
sha2      = "0.10"
